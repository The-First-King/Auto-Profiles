<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mine.autoprofiles">

    <!-- Permissions for cell tower monitoring -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!-- Permissions for calendar access -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <!-- Permissions for profile switching -->
    <uses-permission android:name="lineageos.permission.MANAGE_REMOTE_PREFERENCES" />

    <!-- Background execution -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Optional: For vibration feedback -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Profile Manager Service -->
        <service
            android:name=".services.ProfileManagerService"
            android:exported="false" />

        <!-- Trigger Monitor Service -->
        <service
            android:name=".services.TriggerMonitorService"
            android:exported="false" />

        <!-- Boot Completed Receiver -->
        <receiver
            android:name=".receivers.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Cell Tower Change Receiver -->
        <receiver
            android:name=".receivers.CellTowerReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SERVICE_STATE" />
            </intent-filter>
        </receiver>

        <!-- Time/Alarm Receiver -->
        <receiver
            android:name=".receivers.ScheduleReceiver"
            android:exported="true" />

    </application>

</manifest>
